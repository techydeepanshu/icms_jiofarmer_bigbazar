{
  "name": "inventory-management",
  "version": "1.0.0",
  "description": "manage pos inventory using tesseract",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon index.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "nodemonConfig": {
    "ignore": [
      "client/*"
    ]
  },
  "author": "alekh",
  "license": "ISC",
  "dependencies": {
    "aws-textract-helper": "^1.0.0-beta.5",
    "child_process": "^1.0.2",
    "concurrently": "^5.3.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "jsonwebtoken": "^8.5.1",
    "multer": "^1.4.2",
    "request": "^2.88.2"
  }
}
